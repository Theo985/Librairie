--[[
    FlushLib.lua
    A black & green UI library for Roblox Luau.
    Features: Side "COMBAT" banner, draggable window, buttons, toggles, sliders, notifications.
]]

local FlushLib = {}
FlushLib.__index = FlushLib

-- Create ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FlushHubUI"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

if syn and syn.protect_gui then syn.protect_gui(ScreenGui) end

-- Main Window Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainContainer"
MainFrame.Size = UDim2.new(0, 440, 0, 560)
MainFrame.Position = UDim2.new(0.5, -220, 0.5, -280)
MainFrame.BackgroundColor3 = Color3.fromRGB(6, 10, 6)
MainFrame.BackgroundTransparency = 0.05
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true

-- Green outline
local Outline = Instance.new("UIStroke")
Outline.Color = Color3.fromRGB(0, 255, 80)
Outline.Thickness = 1.8
Outline.Transparency = 0.3
Outline.Parent = MainFrame

-- Rounded corners
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 14)
Corner.Parent = MainFrame

-- Top Bar (draggable + title)
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 44)
TopBar.BackgroundColor3 = Color3.fromRGB(0, 8, 0)
TopBar.BackgroundTransparency = 0.2
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 14)
TopCorner.Parent = TopBar

-- Title "FLUSH HUB" in green
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -80, 1, 0)
TitleLabel.Position = UDim2.new(0, 16, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "FLUSH HUB"
TitleLabel.TextColor3 = Color3.fromRGB(80, 255, 80)
TitleLabel.TextSize = 22
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Parent = TopBar

-- Close button (small green X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 32, 0, 32)
CloseBtn.Position = UDim2.new(1, -42, 0, 6)
CloseBtn.BackgroundColor3 = Color3.fromRGB(20, 40, 20)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
CloseBtn.TextSize = 20
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.BorderSizePixel = 0
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn
CloseBtn.Parent = TopBar

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- Side Panel (combat branding)
local SidePanel = Instance.new("Frame")
SidePanel.Size = UDim2.new(0, 100, 1, 0)
SidePanel.Position = UDim2.new(0, 0, 0, 44)
SidePanel.BackgroundColor3 = Color3.fromRGB(0, 8, 0)
SidePanel.BackgroundTransparency = 0.3
SidePanel.BorderSizePixel = 0
SidePanel.Parent = MainFrame

local SideCorner = Instance.new("UICorner")
SideCorner.CornerRadius = UDim.new(0, 0)
SideCorner.Parent = SidePanel

-- Vertical "COMBAT" text
local CombatLabel = Instance.new("TextLabel")
CombatLabel.Size = UDim2.new(1, 0, 1, 0)
CombatLabel.BackgroundTransparency = 1
CombatLabel.Text = "COMBAT"
CombatLabel.TextColor3 = Color3.fromRGB(60, 200, 60)
CombatLabel.TextSize = 28
CombatLabel.TextWrapped = true
CombatLabel.Font = Enum.Font.GothamBlack
CombatLabel.Rotation = 90
CombatLabel.Parent = SidePanel

-- Subtle glow effect
local Glow = Instance.new("Frame")
Glow.Size = UDim2.new(1, 0, 1, 0)
Glow.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
Glow.BackgroundTransparency = 0.92
Glow.BorderSizePixel = 0
Glow.Parent = SidePanel

-- Content Area (right side)
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -110, 1, -56)
ContentFrame.Position = UDim2.new(0, 108, 0, 52)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

-- Scrollable container for UI elements
local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, 0, 1, 0)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.BorderSizePixel = 0
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.ScrollBarImageColor3 = Color3.fromRGB(80, 200, 80)
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollingFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollingFrame.Parent = ContentFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 12)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Parent = ScrollingFrame

-- Function to add a section title
function FlushLib:Section(title)
    local section = Instance.new("TextLabel")
    section.Size = UDim2.new(1, -20, 0, 32)
    section.BackgroundColor3 = Color3.fromRGB(10, 20, 10)
    section.BackgroundTransparency = 0.4
    section.Text = "   " .. string.upper(title)
    section.TextColor3 = Color3.fromRGB(100, 255, 100)
    section.TextSize = 16
    section.Font = Enum.Font.GothamSemibold
    section.TextXAlignment = Enum.TextXAlignment.Left
    local sectionCorner = Instance.new("UICorner")
    sectionCorner.CornerRadius = UDim.new(0, 6)
    sectionCorner.Parent = section
    section.Parent = ScrollingFrame
    return section
end

-- Function to add a button
function FlushLib:Button(text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 42)
    btn.BackgroundColor3 = Color3.fromRGB(12, 20, 12)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(140, 255, 140)
    btn.TextSize = 15
    btn.Font = Enum.Font.Gotham
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 8)
    btnCorner.Parent = btn
    local btnStroke = Instance.new("UIStroke")
    btnStroke.Color = Color3.fromRGB(0, 180, 0)
    btnStroke.Thickness = 1
    btnStroke.Transparency = 0.5
    btnStroke.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        if callback then callback() end
        -- Visual feedback
        btn.BackgroundColor3 = Color3.fromRGB(30, 60, 30)
        task.wait(0.1)
        btn.BackgroundColor3 = Color3.fromRGB(12, 20, 12)
    end)
    btn.Parent = ScrollingFrame
    return btn
end

-- Function to add a toggle (returns the toggle state and a setter)
function FlushLib:Toggle(labelText, default, callback)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -20, 0, 44)
    container.BackgroundColor3 = Color3.fromRGB(8, 16, 8)
    container.BackgroundTransparency = 0.2
    local containerCorner = Instance.new("UICorner")
    containerCorner.CornerRadius = UDim.new(0, 8)
    containerCorner.Parent = container
    container.Parent = ScrollingFrame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, -10, 1, 0)
    label.Position = UDim2.new(0, 12, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(180, 255, 180)
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.Parent = container
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 50, 0, 28)
    toggleBtn.Position = UDim2.new(1, -62, 0.5, -14)
    toggleBtn.BackgroundColor3 = default and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 40)
    toggleBtn.Text = default and "ON" or "OFF"
    toggleBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    toggleBtn.TextSize = 13
    toggleBtn.Font = Enum.Font.GothamBold
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 12)
    toggleCorner.Parent = toggleBtn
    toggleBtn.Parent = container
    
    local state = default
    toggleBtn.MouseButton1Click:Connect(function()
        state = not state
        toggleBtn.BackgroundColor3 = state and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 40)
        toggleBtn.Text = state and "ON" or "OFF"
        if callback then callback(state) end
    end)
    
    return {
        get = function() return state end,
        set = function(newState)
            state = newState
            toggleBtn.BackgroundColor3 = state and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 40)
            toggleBtn.Text = state and "ON" or "OFF"
            if callback then callback(state) end
        end
    }
end

-- Function to add a slider (value between min and max)
function FlushLib:Slider(labelText, min, max, default, callback)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -20, 0, 70)
    container.BackgroundColor3 = Color3.fromRGB(8, 16, 8)
    container.BackgroundTransparency = 0.2
    local containerCorner = Instance.new("UICorner")
    containerCorner.CornerRadius = UDim.new(0, 8)
    containerCorner.Parent = container
    container.Parent = ScrollingFrame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 22)
    label.Position = UDim2.new(0, 12, 0, 6)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(200, 255, 200)
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.Parent = container
    
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0, 50, 0, 22)
    valueLabel.Position = UDim2.new(1, -60, 0, 6)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = tostring(default)
    valueLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    valueLabel.TextSize = 14
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.Parent = container
    
    local sliderBar = Instance.new("Frame")
    sliderBar.Size = UDim2.new(1, -24, 0, 4)
    sliderBar.Position = UDim2.new(0, 12, 0, 40)
    sliderBar.BackgroundColor3 = Color3.fromRGB(30, 50, 30)
    local barCorner = Instance.new("UICorner")
    barCorner.CornerRadius = UDim.new(0, 2)
    barCorner.Parent = sliderBar
    sliderBar.Parent = container
    
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(0, 220, 0)
    fill.BorderSizePixel = 0
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(0, 2)
    fillCorner.Parent = fill
    fill.Parent = sliderBar
    
    local grabber = Instance.new("TextButton")
    grabber.Size = UDim2.new(0, 16, 0, 16)
    grabber.Position = UDim2.new((default - min) / (max - min), -8, 0.5, -8)
    grabber.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    grabber.Text = ""
    local grabCorner = Instance.new("UICorner")
    grabCorner.CornerRadius = UDim.new(0, 8)
    grabCorner.Parent = grabber
    grabber.Parent = sliderBar
    
    local dragging = false
    local value = default
    
    local function updateSlider(inputPos)
        local relative = (inputPos.X - sliderBar.AbsolutePosition.X) / sliderBar.AbsoluteSize.X
        local newValue = math.clamp(min + (max - min) * relative, min, max)
        value = newValue
        local percent = (value - min) / (max - min)
        fill.Size = UDim2.new(percent, 0, 1, 0)
        grabber.Position = UDim2.new(percent, -8, 0.5, -8)
        valueLabel.Text = string.format("%.1f", value)
        if callback then callback(value) end
    end
    
    grabber.MouseButton1Down:Connect(function()
        dragging = true
        local mouse = game:GetService("UserInputService")
        local connection
        connection = mouse.InputChanged:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
                updateSlider(input.Position)
            end
        end)
        local releaseConn
        releaseConn = mouse.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = false
                connection:Disconnect()
                releaseConn:Disconnect()
            end
        end)
    end)
    
    return {
        get = function() return value end,
        set = function(newVal)
            value = math.clamp(newVal, min, max)
            local percent = (value - min) / (max - min)
            fill.Size = UDim2.new(percent, 0, 1, 0)
            grabber.Position = UDim2.new(percent, -8, 0.5, -8)
            valueLabel.Text = string.format("%.1f", value)
            if callback then callback(value) end
        end
    }
end

-- Notifications (black & green)
function FlushLib:Notify(message, duration)
    duration = duration or 3
    local notif = Instance.new("Frame")
    notif.Size = UDim2.new(0, 280, 0, 48)
    notif.Position = UDim2.new(1, -300, 0, 20)
    notif.BackgroundColor3 = Color3.fromRGB(0, 8, 0)
    notif.BackgroundTransparency = 0.1
    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 10)
    notifCorner.Parent = notif
    local notifStroke = Instance.new("UIStroke")
    notifStroke.Color = Color3.fromRGB(0, 200, 0)
    notifStroke.Thickness = 1
    notifStroke.Parent = notif
    
    local text = Instance.new("TextLabel")
    text.Size = UDim2.new(1, -16, 1, 0)
    text.Position = UDim2.new(0, 8, 0, 0)
    text.BackgroundTransparency = 1
    text.Text = message
    text.TextColor3 = Color3.fromRGB(150, 255, 150)
    text.TextSize = 14
    text.Font = Enum.Font.Gotham
    text.TextWrapped = true
    text.Parent = notif
    
    notif.Parent = ScreenGui
    
    -- Animation
    notif:TweenPosition(UDim2.new(1, -300, 0, 20), "Out", "Quad", 0.3, true)
    task.wait(duration)
    notif:TweenPosition(UDim2.new(1, 0, 0, 20), "Out", "Quad", 0.3, true)
    task.wait(0.35)
    notif:Destroy()
end

-- Draggable functionality for MainFrame
local UserInputService = game:GetService("UserInputService")
local draggingEnabled = false
local dragStart, startPos

TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingEnabled = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if draggingEnabled and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingEnabled = false
    end
end)

-- Open / Close API
function FlushLib:Open()
    MainFrame.Visible = true
end

function FlushLib:Close()
    MainFrame.Visible = false
end

function FlushLib:Toggle()
    MainFrame.Visible = not MainFrame.Visible
end

-- Initialize UI
MainFrame.Parent = ScreenGui
ScreenGui.Parent = game:GetService("CoreGui")

-- Prevents errors if CoreGui is not available (for some executors)
pcall(function()
    ScreenGui.Parent = game:GetService("CoreGui")
end)

return FlushLib
